[package]
name = "errorscript-lsp"
version = "0.0.0"
edition = "2024"
description = "ErrorScript language server"
license = "MIT"
repository = "https://github.com/kossnocorp/errorscript"

[lib]
crate-type = ["cdylib"]

[dependencies]
errorscript = { path = "../crate" }
napi = { version = "3", features = ["tokio_rt"] }
napi-derive = "3"
tokio = { version = "1", features = ["io-std", "macros", "sync", "time"] }
tower = { version = "0.4", features = ["util"] }
tower-lsp = "0.20"
serde = { version = "1", features = ["derive"] }

[build-dependencies]
napi-build = "2"
